/* MainTest.css */

.mainNav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    background-color: #fff; /* 배경색을 흰색(#fff)으로 변경 */
    color: #000; /* 텍스트 색상을 검은색(#000)으로 변경 */
}

.Title {
    font-size: 100px;
    font-weight: bold;
}

.leftBox {
    display: flex;
    align-items: center;
}

/* AboutUs, MyPage, LoginBox, Logout를 세로 방향으로 정렬하고 아이콘과 텍스트를 가운데 정렬하세요 */
.AboutUS,
.Mypage,
.LoginBox,
.Login,
.Logout {
    display: flex;
    flex-direction: column; /* 세로 방향으로 정렬 */
    align-items: center;
    margin-right: 10px; /* 오른쪽 여백을 원하는 크기로 조정하세요 */
}

/* AboutUs, MyPage, LoginBox 사이의 간격을 10px로 조정하세요 */
.AboutUS,
.Mypage {
    margin-right: 100px; /* 원하는 간격으로 조정하세요 */
}

/* 아이콘 색상을 원하는 대로 변경하세요 */
.AboutUS svg,
.Mypage svg,
.Login svg,
.Logout svg {
    font-size: 70px; /* 아이콘 사이즈를 조정하세요 */
    margin-bottom: 5px; /* 아이콘 아래에 조금 여백 추가 */
    color: #000; /* 아이콘 색상을 검은색(#000)으로 변경 */
}

/* 텍스트 색상을 다시 설정하세요 */
.AboutUS p,
.Mypage p,
.Login p,
.Logout p {
    color: #000; /* 텍스트 색상을 검은색(#000)으로 변경 */
    font-size: 30px;
}

/* 호버 효과를 추가하세요 */
.Title:hover,
.AboutUS:hover,
.Mypage:hover,
.LoginBox:hover,
.Login:hover,
.Logout:hover {
    cursor: pointer; /* 호버 시 커서를 손가락 모양으로 변경합니다 */
    opacity: 0.8; /* 호버 시 투명도를 조정하세요 */
}
